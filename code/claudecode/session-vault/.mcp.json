{
  "mcpServers": {
    "session-vault": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/server.js"],
      "env": {
        "VAULT_DIR": "${HOME}/.session-vault"
      }
    }
  }
}
